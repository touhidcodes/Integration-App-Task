// "use server";

// import { signIn, signOut } from "../../lib/auth";

// export async function socialLogin() {
//   await signIn({ redirectTo: "/integrations" });
// }

// export async function logout() {
//   await signOut({ redirectTo: "/" });
// }
